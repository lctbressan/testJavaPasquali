/*


const axios = require('axios');

const baseURL = 'https://jsonplaceholder.typicode.com';

    // Function to make a GET r
        // equest to retrieve posts
    async function getPosts() {
        try {
    const response = await axios.get(`${baseURL}/posts`);
            console.log('GET Posts Response:', response.data);
        } catch (error) {
            console.error('GET Posts Error:', error);
        }
    }

    // Function to make a POST request to create a new post
    async function createPost() {
  const newPost = {
                title: 'Test Post',
                body: 'This is a test post.',
                userId: 1, // You can specify the user ID here
  };

        try {
    const response = await axios.post(`${baseURL}/posts`, newPost);
            console.log('POST Create Post Response:', response.data);
        } catch (error) {
            console.error('POST Create Post Error:', error);
        }
    }

    // Call the functions to perform the actions
    getPosts();
    createPost();


}
*/
/*


const axios = require('axios');

const baseURL = 'https://jsonplaceholder.typicode.com';

    // Function to make a GET r
        // equest to retrieve posts
    async function getPosts() {
        try {
    const response = await axios.get(`${baseURL}/posts`);
            console.log('GET Posts Response:', response.data);
        } catch (error) {
            console.error('GET Posts Error:', error);
        }
    }

    // Function to make a POST request to create a new post
    async function createPost() {
  const newPost = {
                title: 'Test Post',
                body: 'This is a test post.',
                userId: 1, // You can specify the user ID here
  };

        try {
    const response = await axios.post(`${baseURL}/posts`, newPost);
            console.log('POST Create Post Response:', response.data);
        } catch (error) {
            console.error('POST Create Post Error:', error);
        }
    }

    // Call the functions to perform the actions
    getPosts();
    createPost();


}
*/
